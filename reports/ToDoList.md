# Updated To-Do List

1. Add IP-based persistence UI (resume latest countdown for current IP).
2. Implement tokenized share links (avoid raw deadline in URL).
3. Add created_at and timezone fields to the database schema.
4. Add tests for share links, DB resume, and expired state UI.
5. Add README run instructions and environment setup notes.
6. Add optional history page for past countdowns.
